action_server:
  url: "http://localhost:5055/webhook"

tracker_store:
  type: "SQL"
  dialect: "sqlite"  # the dialect used to connect to the database
  db: "tracker.db"  # path to the database file

event_broker:
  type: "pika"
  url: "amqp://guest:guest@localhost:5672/"