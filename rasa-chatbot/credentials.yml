language: yaml
# This file contains the credentials for external services used by the chatbot.

# Example configuration for a messaging platform (e.g., Slack)
# slack:
#   token: "YOUR_SLACK_TOKEN"

# Example configuration for Facebook Messenger
# facebook:
#   verify: "YOUR_VERIFY_TOKEN"
#   secret: "YOUR_APP_SECRET"
#   access_token: "YOUR_PAGE_ACCESS_TOKEN"